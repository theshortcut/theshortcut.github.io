import AppLayout from '@/components/app-layout';
import React, { FC } from 'react';

const AboutPage: FC = () => {
  return <AppLayout>This is the about page</AppLayout>;
};
export default AboutPage;
